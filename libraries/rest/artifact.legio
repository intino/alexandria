dsl Legio

Artifact("io.intino.alexandria", "3.0.0") rest
	Imports
		Compile("io.intino.alexandria", "http", "1.0.0")
		Compile("io.intino.alexandria", "json", "1.0.1")
	Package(mode = ModulesAndLibrariesLinkedByManifest)
	Distribution(release = intino.release)

Repository(identifier = "intino-maven") intino
	Release("https://artifactory.intino.io/artifactory/releases")
	Release("https://artifactory.intino.io/artifactory/release-libraries")release