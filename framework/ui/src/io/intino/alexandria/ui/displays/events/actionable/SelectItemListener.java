package io.intino.alexandria.ui.displays.events.actionable;

import io.intino.alexandria.ui.displays.events.collection.SelectItemEvent;

public interface SelectItemListener {
	void accept(SelectItemEvent event);
}
