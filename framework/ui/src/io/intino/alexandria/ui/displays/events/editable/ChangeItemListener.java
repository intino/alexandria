package io.intino.alexandria.ui.displays.events.editable;

import io.intino.alexandria.ui.displays.events.ChangeItemEvent;

public interface ChangeItemListener {
	void accept(ChangeItemEvent event);
}
