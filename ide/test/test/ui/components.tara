dsl Konos

Service ComponentsTest as UI(title = "", favicon = "", port = "{port}")
	theme = theme1
	Resource("/") home as BlankPage(display=main) Main

Theme theme1
	Palette
		Primary("indigo")
		Secondary("black")
		Error("red")

	Style a
		Margin("10px")

Root main
	Block(style=theme1.a) bloque1 as Horizontal Flexible
		Block bloque21
			Block bloque31
				Text(format=H1, mode=Uppercase) text
			Block bloque32
				Text(format=H1, mode=Uppercase) text2

		Block bloque22
			Text(format=H1, mode=Uppercase) text3

			Panels panels
				Panel textPanel
					Text value > label = "título del campo"

		Selector menu as Menu
			Text(value="text") panel1

		Block selectorContainer as SelectorContainer(bloque1.menu)
			Block panel1
				Text value > label = "título del campo"
				Text valueEditable as Editable
