dsl Konos

Client(domain = "org.siani", clientID = "") monentia as Ness(url = "", user = "", password = "")
	Mounter(schema = opinion)
	Mounter(schema = empty) inputProcesses

Schema opinion
	Attribute asas as Text