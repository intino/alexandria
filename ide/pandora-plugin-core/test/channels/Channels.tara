dsl Pandora

Service monet as JMS
	Request(queue = "catalogs") catalogs
		Parameter catalogs as Object(schema = CategorizationCatalog)
		Parameter name as Text
		Parameter fileOption as File
		Response as Bool

	Request(queue = "processes") processes
		Parameter entryPoint as File
		Response as Object(Processes)
		Exception(code = BadRequest) as Text

	Request(queue = "processes") binaries
		Parameter type as Text
		Response as File

	Request(queue = "empty") emptyRequest
		Response
	Notification(queue = "") send
		Parameter text as Text
		Parameter number as Real
		Parameter value as Integer
		Parameter file as File

	Notification(queue = "") sample
		Parameter text as Text
		Parameter number as Real

Schema CategorizationCatalog
	Attribute name as Text
	Attribute label as Text
	Schema category
		Attribute name as Text
		Attribute label as Text

Schema Processes
	Attribute name as Text
	Attribute label as Text

Queue(id = "catalogs") opinions
	Message(messageType = "bool") as Bool // TODO si no lo pones o pones varios no se queja ERROORRR

Topic(id = "processes") inputProcesses
	Message(messageType = "processes") as Object(schema = Processes) // TODO meter type en el properties

