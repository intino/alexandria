def type(app)
	import React from "react";
    import ReactDOM from "react-dom";
    $use+import...[$NL]

    $use+render...[$NL$NL]
end

def type(use) trigger(import)
    import $value from "../src/pages/$value";
end

def type(use) trigger(render)
    const $use+firstLowerCase = document.getElementById("$use");
    if ($use+firstLowerCase) ReactDOM.render(<$use />, homePage);
end