def type(main)
	package $package;

	import io.intino.konos.alexandria.Box;

	public class Main {
		public static void main(String$[] args) {
			Box box = new $name+SnakeCaseToCamelCase+firstUpperCase~Box(args).open();
			Runtime.getRuntime().addShutdownHook(new Thread(box::close));
		}
	}
end
