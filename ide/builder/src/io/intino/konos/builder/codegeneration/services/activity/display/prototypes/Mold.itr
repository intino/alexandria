def type(mold)
	package $package.displays;

    import io.intino.konos.alexandria.activity.services.push.ActivitySession;
    import $package+validPackage.$box+firstUpperCase~Box;

    import java.net.URL;
    import java.util.List;

    public class $name+FirstUpperCase extends Abstract$name+FirstUpperCase {

    	public $name+FirstUpperCase($box+FirstUpperCase~Box box) {
    		super(box);
    	}

    	public static class Stamps {
			$block+stamps...[$NL$NL]
    	}

    	public static class Blocks {
    		$block...[$NL]
		}
    }
end

def type(block) trigger(stamps)
	$stamp...[$NL]
	$block+stamps...[$NL]
end

def trigger(block)
	$hidden
	$block...[$NL]
end

def trigger(hidden)
	public static class $name+FirstUpperCase {
		public static boolean hidden($moldClass $moldClass+shortType+firstLowerCase) {
			return false;//TODO
		}
	}
end

def type(stamp & openDialogOperation)
	public static class $name+FirstUpperCase {
		$common

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & openExternalDialogOperation)
	public static class $name+FirstUpperCase {
		$common

		public static String dialogPath($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}

		public static String dialogTitle($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//by default is set to operation label
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & openCatalogOperation)
	public static class $name+FirstUpperCase {
		$common

		[$catalogFilter]

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & downloadOperation)
	public static class $name+FirstUpperCase {
		$common

		public static io.intino.konos.alexandria.activity.Resource execute($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, String option, ActivitySession session) {
			return null;//TODO
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & previewOperation)
	public static class $name+FirstUpperCase {
		$common

		public static java.net.URL preview($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & taskOperation)
	public static class $name+FirstUpperCase {
		$common

		public static io.intino.konos.alexandria.activity.model.mold.stamps.operations.TaskOperation.Refresh execute($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, String selfId, ActivitySession session) {
			return io.intino.konos.alexandria.activity.model.mold.stamps.operations.TaskOperation.Refresh.None;
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & exportOperation)
	public static class $name+FirstUpperCase {
		$common

		public static io.intino.konos.alexandria.activity.Resource execute($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, java.time.Instant from, java.time.Instant to, String option, ActivitySession session) {
			return null;//TODO
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end


def type(stamp & location)
	public static class $name+FirstUpperCase {
		$common

		public static java.net.URL icon($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}

		public static String drawingColor($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return null;//TODO
		}
	}
end

def type(stamp & catalogLink)
	public static class $name+FirstUpperCase {
		$common

		[$filter]
		[$itemLoader]
	}
end

def type(stamp & highlight)
	public static class $name+FirstUpperCase {
		$common

		public static String color($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return "";//TODO
		}
	}
end

def type(stamp & embeddedDisplay)
	public static class $name+FirstUpperCase {
		$common

		$displayBuilder
	}
end

def type(stamp & catalogTimeRange)
	public static class $name+FirstUpperCase {
		$common
	}
end

def type(stamp & catalogTimeRangeNavigator)
	public static class $name+FirstUpperCase {
		$common
	}
end

def type(stamp & catalogTime)
	public static class $name+FirstUpperCase {
		$common
	}
end

def type(stamp & catalogTimeNavigator)
	public static class $name+FirstUpperCase {
		$common
	}
end

def type(stamp & embeddedCatalog)
	public static class $name+FirstUpperCase {
		$common

		[$catalogFilter]
	}
end

def type(stamp & icon)
	public static class $name+FirstUpperCase {
		$common

		public static String title($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return "";//TODO
		}
	}
end

def type(stamp & itemLinks)
	public static class $name+FirstUpperCase {
		$common

		public static String title($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
			return "";//TODO
		}
	}
end


def type(stamp)
	public static class $name+FirstUpperCase {
		$common
	}
end

def trigger(common)

	$valueMethod[

	$editable][

	$style
	]
end

def trigger(style)
	public static String style($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
		return "";//TODO
	}
end

def trigger(catalogFilter)
	public static boolean filter($box~Box box, io.intino.konos.alexandria.activity.model.Element element, $moldClass $moldClass+shortType+firstLowerCase, Object target, ActivitySession session) {
		return true;//TODO
	}
end

def trigger(filter)
	public static boolean filter($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, Object target, ActivitySession session) {
		return true;//TODO
	}
end

def trigger(itemLoader)
	public static String item($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
		return null;//TODO return item to open
	}
end

def trigger(displayBuilder)
	public static io.intino.konos.alexandria.activity.displays.AlexandriaStamp buildDisplay($box~Box box, String name, ActivitySession session) {
		return null;//TODO
	}
end

def trigger(editable)
	public static io.intino.konos.alexandria.activity.model.mold.Stamp.Editable.Refresh onChange($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, String value, ActivitySession session) {
		return null;
	}
end

def trigger(valueMethod)
	public static $valueType value($box~Box box, $moldClass $moldClass+shortType+firstLowerCase, ActivitySession session) {
		return null;//TODO
	}
end

def attribute(resource) trigger(valueType)
	java.net.URL
end

def attribute(Breadcrumbs) trigger(valueType)
	io.intino.konos.alexandria.activity.model.mold.stamps.Tree
end

def attribute(CardWallet) trigger(valueType)
	io.intino.konos.alexandria.activity.model.mold.stamps.Wallet
end

def attribute(ItemLinks) trigger(valueType)
	io.intino.konos.alexandria.activity.model.mold.stamps.Links
end

def attribute(Picture) trigger(valueType)
	java.util.List<java.net.URL>
end

def attribute(Rating) trigger(valueType)
	java.lang.Double
end

def trigger(valueType)
	java.lang.String
end