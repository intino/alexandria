def type(scheduled)
	package $package+validname.scheduling;

	import forrest.framework.scheduling.*;
	import org.quartz.*;
	import $package+validname.*;
	import tara.magritte.Graph;

	public class $name+firstUpperCase~Action implements ScheduledTrigger {

		public void execute(JobExecutionContext context) throws JobExecutionException {
			private Graph = (Graph) context.getMergedJobDataMap().get("graph");
			$action...[$NL]
		}
	}
end

def type(action)
    new $name~Action(graph).execute();
end