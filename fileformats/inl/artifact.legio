dsl Legio

Artifact("io.intino.alexandria", "2.0.0") inl
	Imports
		Compile("io.intino.alexandria", "primitives", "1.0.3")
		Test("junit", "junit", "LATEST")
		Test("org.assertj", "assertj-core", "3.12.2")

	Package(ModulesAndLibrariesLinkedByManifest, attachSources = true)
	Distribution(intino.releases)

Repository("intino-maven") intino
	Release("https://artifactory.intino.io/artifactory/release-libraries") releases