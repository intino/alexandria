const PushService = (function () {
    var webSocket;

    return {
        openConnection : (url) => {},
        listen : (callback) => {},
        send : (message) => {}
    };
})();

export default PushService;