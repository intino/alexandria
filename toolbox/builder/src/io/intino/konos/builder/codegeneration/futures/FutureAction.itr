def type(action)
	package $package+validPackage.actions;

	import $package+validPackage.$box+snakecaseToCamelCase+firstUpperCase~Box;
	import io.intino.alexandria.future.Future;
	import java.time.*;
	import java.util.*;
	$schemaImport

	public class $name+snakecaseToCamelCase+firstUpperCase~Future extends Future {
		private $box+snakecaseToCamelCase+firstUpperCase~Box box;

		public $name+snakecaseToCamelCase+firstUpperCase~Future($box+snakecaseToCamelCase+firstUpperCase~Box box, String id) {
			super(box, id);
		}

		public void execute($parameter...[, ]) {

		}
	}
end