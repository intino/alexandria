def type(future)
	package $package+validPackage.agenda;

	import $package+validPackage.$box+snakecaseToCamelCase+firstUpperCase~Box;
	$schemaImport

	public class $name+FirstUpperCase extends Abstract$name+FirstUpperCase {
		private final $box+snakecaseToCamelCase+firstUpperCase~Box box;

		public $name+FirstUpperCase($box+snakecaseToCamelCase+firstUpperCase~Box box) {
			this.box = box;
		}

		$option...[$NL$NL]

		@Override
		protected void onTimeout($parameter+signature...[, ]) {

		}
	}
end

def trigger(option)
	@Override
	protected void on$name+firstUpperCase($parameter+signature...[, ][, $optionParameter+signature...[, ]]) {

	}
end

def trigger(signature)
	$type $name
end

def trigger(names)
	$name
end

def type(schemaImport)
	import $package.schemas.*;
end