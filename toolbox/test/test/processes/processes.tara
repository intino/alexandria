dsl Konos

Service taskManager as TaskInbox
	Inbox helloWorldInbox as ProcessTrigger(helloWorld)
		Input input as Object(Budget)
    	Output as LongInteger


Process(bpmn = "service.bpmn") service

Process(bpmn = "helloworld.bpmn") helloWorld

Schema Budget
	Attribute total as Real

MessageHub messageHub as JmsBus(url = "tcp://localhost:63000", user = "user1", password = "1234", clientId = "")
