import React from "react";
import PropTypes from "prop-types";

class PassiveView extends React.Component {

    constructor(props) {
        super(props);
    };

}

PassiveView.propTypes = {
    id: PropTypes.string.isRequired
};

export default PassiveView;